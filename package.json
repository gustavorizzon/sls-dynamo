{
  "name": "sls-dynamo",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
		"invoke": "sls invoke -f hero-insert --path requests-mock/hero.insert.json",
		"invoke:local": "sls invoke local -f hero-insert --path requests-mock/hero.insert.json",
		"invoke-trigger": "sls invoke -f hero-trigger --path requests-mock/hero.trigger.json",
		"invoke-trigger:local": "sls invoke local -f hero-trigger --path requests-mock/hero.trigger.json"
	},
  "keywords": [],
  "author": "Gustavo",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "aws-sdk": "^2.1061.0"
  }
}
